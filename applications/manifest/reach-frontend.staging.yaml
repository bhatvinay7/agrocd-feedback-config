apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: reach-frontend
  namespace: argocd
spec:
  project: reach-frontend
  source:
    repoURL: https://github.com/bhatvinay7/feedback.git 
    targetRevision: staging
    path: Helm/chart.staging/services/reach-frontend
    helm:
      valueFiles:
        - values.staging.yml
  destination:
      server: https://kubernetes.default.svc
      namespace: feedback-services
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true